{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "enableAllProjectMcpServers": true,
  "permissions": {
    "allow": [
      "Bash(npm run format 2>&1)",
      "Bash(npm run format 2>&1 && npm run lint 2>&1)",
      "Bash(npm run lint 2>&1)",
      "Bash(npx playwright test:*)"
    ]
  }
}
